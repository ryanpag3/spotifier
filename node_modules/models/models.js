module.exports = {
    // support legacy models
    User: require('../../spotifier/server/models/user'),
    Artist: require('../../spotifier/server/models/artist')
    // add more as needed, keep centralized within module directory
}